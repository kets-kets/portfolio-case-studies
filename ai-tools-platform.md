# 🤖 AI Tools Platform: Case Study

> **Тип проекта**: Enterprise Web Application  
> **Статус**: Production Ready  
> **Период разработки**: 2025-2026  
> **Роль**: Full-Stack Developer, Cloud Architect

---

## 📋 О Проекте

**AI Tools Platform** — корпоративная платформа для унифицированного доступа к генеративным AI-моделям. Система агрегирует несколько провайдеров (Replicate, OpenAI, Vertex AI) под единым интерфейсом с централизованной аутентификацией, системой кредитов и rate limiting.

---

## 🎯 Проблема

### Бизнес-контекст
Организация использовала множество AI-сервисов (генерация изображений, текста, видео), что приводило к:
- **Фрагментации доступа**: Каждый сотрудник управлял своими API-ключами
- **Отсутствию контроля затрат**: Невозможно отследить расходы по департаментам
- **Проблемам безопасности**: API-ключи хранились в незащищённых местах
- **Низкой эффективности**: Нет единого интерфейса для разных моделей

### Технические вызовы
1. **Разнородные API**: Каждый провайдер имеет свой формат запросов
2. **Асинхронность**: Генерация может занимать минуты (polling механизм)
3. **Масштабируемость**: Поддержка множества пользователей одновременно
4. **Безопасность**: Корпоративная аутентификация, RBAC, rate limiting

---

## 💡 Решение

### Архитектурный Подход

Реализована **Clean Architecture** с чётким разделением слоёв:

```
┌─────────────────────────────────────────────────────────┐
│  Presentation Layer (React 19)                          │
│  └─ Components, Hooks, Context API                     │
└──────────────────┬──────────────────────────────────────┘
                   │ REST API
┌──────────────────▼──────────────────────────────────────┐
│  Application Layer (Flask)                              │
│  └─ Routes, Middleware, Error Handlers                 │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│  Business Logic Layer (Services)                        │
│  ├─ AuthService (OAuth, JWT, RBAC)                     │
│  ├─ GenerationService (Provider orchestration)         │
│  ├─ DocsService (GCS file operations)                  │
│  └─ StorageService (Local file handling)               │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│  Infrastructure Layer                                   │
│  ├─ SQLAlchemy (ORM)                                   │
│  ├─ Replicate SDK                                      │
│  ├─ Google Cloud Storage                               │
│  └─ Google Secret Manager                              │
└─────────────────────────────────────────────────────────┘
```

### Ключевые Компоненты

#### 1. Provider Abstraction Layer
Унифицированный интерфейс для работы с разными AI-провайдерами:

**Проблема**: Каждый провайдер имеет свой API (Replicate, OpenAI, Vertex AI)

**Решение**: Абстракция через Pydantic schemas и единый `GenerationService`

**Преимущества**:
- Легко добавить нового провайдера (просто новая конфигурация)
- Клиентский код не зависит от конкретного API
- Централизованная обработка ошибок

#### 2. Асинхронная Генерация
**Проблема**: AI-генерация занимает 10-60 секунд, нельзя блокировать UI

**Решение**: Polling механизм
```
Client → POST /generate → Backend → Replicate API
                            ↓
                       Return task_id
                            ↓
Client → GET /poll/{task_id} (каждые 2 сек)
                            ↓
Backend → Check Replicate status
                            ↓
                  Return result when ready
```

**Преимущества**:
- UI остаётся отзывчивым
- Пользователь видит прогресс
- Возможность отмены генерации

#### 3. Credits System
**Проблема**: Контроль затрат на AI-генерацию

**Решение**: Внутренняя валюта (credits)
- Каждая модель имеет фиксированную стоимость
- Баланс пользователя проверяется перед генерацией
- Автоматический refund при ошибке

**Преимущества**:
- Прозрачность затрат
- Квоты для разных ролей (user/manager/admin)
- Аналитика использования

#### 4. Security-First Подход

**Аутентификация**:
- Google OAuth 2.0 (корпоративный домен)
- JWT tokens (24h expiry)
- RBAC (admin/manager/user)

**Rate Limiting** (Nginx):
```nginx
/api/generate:   5 req/sec  (burst 10)
/api/*:         20 req/sec  (burst 50)
```

**Security Headers**:
- Content Security Policy (CSP)
- X-Frame-Options: SAMEORIGIN
- X-XSS-Protection
- IP Whitelist для `/admin`

---

## 🛠️ Технологический Стек

### Frontend
- **React 19**: Компонентная архитектура
- **Vite**: Build tool с HMR
- **TailwindCSS**: Utility-first styling
- **Custom Hooks**: `useGeneration`, `useHistory`, `useAdminUsers`
- **Context API**: Глобальное состояние аутентификации

### Backend
- **Python 3.11**: Type-safe с MyPy strict mode
- **Flask**: Lightweight web framework
- **SQLAlchemy**: ORM для работы с БД
- **Gunicorn**: Production WSGI server
- **Replicate SDK**: AI model provider

### Infrastructure
- **Google Cloud Platform**:
  - Compute Engine (e2-standard-2)
  - Cloud Storage (GCS)
  - Secret Manager
- **Docker Compose**: Multi-container orchestration
- **Nginx**: Reverse proxy + rate limiting
- **GitHub Actions**: CI/CD pipeline

---

## 🚀 Реализованные Фичи

### Для Пользователей
✅ Единый интерфейс для всех AI-моделей  
✅ История генераций с поиском  
✅ Референсные изображения (character, style, color)  
✅ Real-time прогресс генерации  
✅ Скачивание результатов  

### Для Администраторов
✅ Управление пользователями (добавление, блокировка)  
✅ Управление кредитами (пополнение, списание)  
✅ Назначение ролей (user/manager/admin)  
✅ Аналитика использования  

### DevOps
✅ Автоматический деплой через GitHub Actions  
✅ Health checks для мониторинга  
✅ Structured logging (JSON)  
✅ Database migrations (Alembic)  

---

## 🎯 Решённые Проблемы

### 1. Масштабируемость
**Вызов**: Поддержка множества одновременных пользователей на одной VM

**Решение**:
- Gunicorn с 4 workers (CPU-bound)
- Connection pooling для БД
- GCS для хранения файлов (не локальный диск)
- Rate limiting для защиты от перегрузки

### 2. Безопасность
**Вызов**: Защита корпоративных данных и API-ключей

**Решение**:
- OAuth только с корпоративным доменом
- GCP Secret Manager для секретов (не .env в продакшене)
- RBAC для разграничения доступа
- IP whitelist для админки

### 3. Observability
**Вызов**: Диагностика проблем в production

**Решение**:
- Structured logging (JSON) для парсинга
- Health checks (`/health` endpoint)
- Error tracking с детальными stack traces
- Логирование всех API-запросов

### 4. Developer Experience
**Вызов**: Поддержка кода несколькими разработчиками

**Решение**:
- MyPy strict mode (обязательная типизация)
- Conventional Commits
- Service Layer паттерн (не God Objects)
- Документация в `.agent/` директории

---

## 🔮 Уроки и Выводы

### Что Сработало
✅ **Clean Architecture**: Легко добавлять новые провайдеры  
✅ **Type Safety**: MyPy предотвратил множество багов  
✅ **Docker**: Консистентность между dev и prod  
✅ **CI/CD**: Деплой за 5 минут без downtime  

### Что Можно Улучшить
⚠️ **PostgreSQL вместо SQLite**: Для лучшей масштабируемости  
⚠️ **Redis для кеширования**: Уменьшить нагрузку на БД  
⚠️ **Kubernetes**: Для горизонтального масштабирования  
⚠️ **Unit Tests**: Покрытие тестами <50%  

### Ключевые Инсайты
1. **Provider Abstraction окупается**: Добавление нового провайдера занимает 1 час вместо недели
2. **Security нельзя добавить потом**: OAuth и RBAC были с первого дня
3. **Observability критична**: Structured logging спас множество раз
4. **Type Safety ускоряет разработку**: MyPy предотвращает глупые ошибки

---

## 🔗 Связанные Проекты

- [ai-provider-sdk](https://github.com/kets-kets/ai-provider-sdk) — Выделенный SDK для работы с AI-провайдерами
- [mini-ai-playground](https://github.com/kets-kets/mini-ai-playground) — Упрощённая демо-версия

---

**Автор**: kets  
**GitHub**: [@kets-kets](https://github.com/kets-kets)  
**Telegram**: [@ketsdpt](https://t.me/ketsdpt)
